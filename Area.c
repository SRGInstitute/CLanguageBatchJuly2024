#include <stdio.h>
#define PI  3.14
int main(){
float R,Area;
printf("Enter the value of R\n");
scanf("%f",&R);
Area = float(PI) * R * R;
printf("The Area of a circle is =%f\n,Area",Area);
return 0;
}

