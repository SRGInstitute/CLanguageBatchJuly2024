#include <stdio.h>
int main(){
int a,b,multiply;
printf("Enter a no of a\n");
scanf("%d",&a);
printf("Enter a no b\n");
scanf("%d",&b);
multiply = a*b;
printf("value of multiply %d",multiply);
return 0;
}
